# 209. Membangun UserModule dengan CRUD endpoint dan relasi ke Post

## Definisi & Target
UserModule menangani manajemen user. Endpoint GET users/:id mengembalikan data user beserta posts yang ditulisnya menggunakan Prisma `include`.

## Komponen & Nest CLI Command
```bash
nest g module users
nest g service users --no-spec
nest g controller users --no-spec
nest g module prisma/prisma --no-spec --flat
```

File yang dibuat/dimodifikasi:
- `src/prisma/prisma.service.ts`, `src/prisma/prisma.module.ts`
- `src/users/users.controller.ts`
- `src/users/users.service.ts`
- `src/users/dto/create-user.dto.ts`

## Kode Implementasi (Per File)
```typescript
// src/users/dto/create-user.dto.ts
import { IsEmail, IsString, MinLength } from 'class-validator';

export class CreateUserDto {
  @IsEmail()
  email: string;

  @IsString()
  @MinLength(3)
  name: string;

  @IsString()
  @MinLength(6)
  password: string;
}
```

```typescript
// src/users/users.service.ts
import { Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { CreateUserDto } from './dto/create-user.dto';

@Injectable()
export class UsersService {
  constructor(private prisma: PrismaService) {}

  async findAll() {
    const users = await this.prisma.user.findMany({
      select: { id: true, email: true, name: true, bio: true, createdAt: true },
    });
    return { data: users };
  }

  async findOne(id: number) {
    const user = await this.prisma.user.findUnique({
      where: { id },
      select: {
        id: true, email: true, name: true, bio: true,
        posts: {
          where: { deletedAt: null },
          select: { id: true, title: true, slug: true, status: true, createdAt: true },
        },
      },
    });
    if (!user) throw new NotFoundException(`User #${id} not found`);
    return { data: user };
  }

  async create(dto: CreateUserDto) {
    const user = await this.prisma.user.create({ data: dto });
    return { data: user, message: 'User created' };
  }
}
```

```typescript
// src/users/users.controller.ts
import { Controller, Get, Post, Param, Body } from '@nestjs/common';
import { UsersService } from './users.service';
import { CreateUserDto } from './dto/create-user.dto';

@Controller('users')
export class UsersController {
  constructor(private readonly usersService: UsersService) {}

  @Get()
  findAll() { return this.usersService.findAll(); }

  @Get(':id')
  findOne(@Param('id') id: string) { return this.usersService.findOne(+id); }

  @Post()
  create(@Body() dto: CreateUserDto) { return this.usersService.create(dto); }
}
```

## Cara Integrasi & Pengujian
```bash
curl http://localhost:3000/users
# Response: { "data": [{ "id": 1, "email": "admin@blog.com", "name": "Admin" }] }

curl http://localhost:3000/users/1
# Response: { "data": { "id": 1, "name": "Admin", "posts": [...] } }
```
