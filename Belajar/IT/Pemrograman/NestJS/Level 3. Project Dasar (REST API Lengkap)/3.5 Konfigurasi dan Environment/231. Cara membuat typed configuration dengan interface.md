# 231. Cara membuat typed configuration dengan interface

## Definisi & Target
Mendefinisikan TypeScript interface untuk konfigurasi memberikan type safety dan autocomplete penuh saat mengakses ConfigService. Ini mencegah typo dan memudahkan refactoring.

## Komponen & Nest CLI Command
Tidak ada CLI command.

## Kode Implementasi (Per File)
```typescript
// src/config/config.interface.ts
export interface DatabaseConfig {
  host: string;
  port: number;
  username: string;
  password: string;
  database: string;
}

export interface JwtConfig {
  secret: string;
  expiration: string;
}

export interface RedisConfig {
  host: string;
  port: number;
}

export interface AppConfiguration {
  port: number;
  nodeEnv: string;
  database: DatabaseConfig;
  jwt: JwtConfig;
  redis: RedisConfig;
}
```

```typescript
// src/config/configuration.ts
import { AppConfiguration } from './config.interface';

export default (): AppConfiguration => ({
  port: parseInt(process.env.PORT, 10) || 3000,
  nodeEnv: process.env.NODE_ENV || 'development',
  database: {
    host: process.env.DB_HOST || 'localhost',
    port: parseInt(process.env.DB_PORT, 10) || 5432,
    username: process.env.DB_USERNAME || 'postgres',
    password: process.env.DB_PASSWORD || 'postgres',
    database: process.env.DB_DATABASE || 'blog_db',
  },
  jwt: {
    secret: process.env.JWT_SECRET || 'default-secret',
    expiration: process.env.JWT_EXPIRATION || '7d',
  },
  redis: {
    host: process.env.REDIS_HOST || 'localhost',
    port: parseInt(process.env.REDIS_PORT, 10) || 6379,
  },
});
```

```typescript
// src/common/decorators/config.decorator.ts
import { Inject } from '@nestjs/common';

export const InjectDatabaseConfig = () => Inject('DATABASE_CONFIG');
export const InjectJwtConfig = () => Inject('JWT_CONFIG');
```

```typescript
// src/database/database.service.ts
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { AppConfiguration, DatabaseConfig } from '../config/config.interface';

@Injectable()
export class DatabaseService {
  private dbConfig: DatabaseConfig;

  constructor(private configService: ConfigService<AppConfiguration>) {
    // Type-safe — autocomplete semua field
    this.dbConfig = {
      host: this.configService.get('database.host', { infer: true }),
      port: this.configService.get('database.port', { infer: true }),
      username: this.configService.get('database.username', { infer: true }),
      password: this.configService.get('database.password', { infer: true }),
      database: this.configService.get('database.database', { infer: true }),
    };
  }

  getConfig(): DatabaseConfig {
    return this.dbConfig;
  }
}
```

## Cara Integrasi & Pengujian
```bash
# Dengan typed config, autocomplete bekerja penuh
# configService.get('database.'); // VS Code menampilkan daftar field
# TypeScript error jika field salah eja
```
