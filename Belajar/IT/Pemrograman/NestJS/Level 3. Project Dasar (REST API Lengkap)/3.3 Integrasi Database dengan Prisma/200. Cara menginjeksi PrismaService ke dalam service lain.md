# 200. Cara menginjeksi PrismaService ke dalam service lain

## Definisi & Target
Setelah PrismaService dibuat sebagai Global module, ia bisa di-inject ke service manapun tanpa perlu import module lagi. Ini memungkinkan semua service mengakses database melalui Prisma Client.

## Komponen & Nest CLI Command
Tidak ada CLI command.

File yang dimodifikasi:
- `src/products/products.service.ts`
- `src/products/products.module.ts`

## Kode Implementasi (Per File)
```typescript
// src/products/products.module.ts
import { Module } from '@nestjs/common';
import { ProductsController } from './products.controller';
import { ProductsService } from './products.service';

@Module({
  controllers: [ProductsController],
  providers: [ProductsService],
})
export class ProductsModule {}
```

```typescript
// src/products/products.service.ts
import { Injectable } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';
import { CreateProductDto } from './dto/create-product.dto';
import { UpdateProductDto } from './dto/update-product.dto';

@Injectable()
export class ProductsService {
  constructor(private prisma: PrismaService) {}

  async findAll() {
    const products = await this.prisma.product.findMany();
    return { data: products };
  }

  async findOne(id: number) {
    const product = await this.prisma.product.findUnique({ where: { id } });
    if (!product) throw new NotFoundException(`Product #${id} not found`);
    return { data: product };
  }

  async create(dto: CreateProductDto) {
    const product = await this.prisma.product.create({ data: dto });
    return { data: product, message: 'Product created' };
  }

  async update(id: number, dto: UpdateProductDto) {
    const product = await this.prisma.product.update({
      where: { id },
      data: dto,
    });
    return { data: product, message: 'Product updated' };
  }

  async remove(id: number) {
    await this.prisma.product.delete({ where: { id } });
    return { message: 'Product deleted' };
  }
}
```

## Cara Integrasi & Pengujian
```bash
# Tanpa import tambahan — PrismaModule sudah global
curl -X POST http://localhost:3000/products -H "Content-Type: application/json" -d '{"name":"Laptop","price":15000,"stock":10}'
# Data tersimpan di database via Prisma
```
