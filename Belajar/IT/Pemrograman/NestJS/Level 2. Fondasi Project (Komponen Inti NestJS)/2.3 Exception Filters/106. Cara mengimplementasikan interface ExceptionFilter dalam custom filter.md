# 106. Cara mengimplementasikan interface ExceptionFilter dalam custom filter

## Definisi & Target
Semua exception filter harus mengimplementasikan interface `ExceptionFilter<T>` dari `@nestjs/common`. Interface ini mewajibkan method `catch(exception: T, host: ArgumentsHost)`. Parameter generic `T` menentukan tipe exception yang ditangani.

## Komponen & Nest CLI Command
```
nest g filter common/exception --no-spec
```
File: `src/common/filters/business-exception.filter.ts`

## Kode Implementasi (Per File)

```typescript
// src/common/exceptions/business.exception.ts
export class BusinessException extends Error {
  constructor(
    public readonly code: string,
    public readonly message: string,
    public readonly statusCode: number = 400,
    public readonly details?: any,
  ) {
    super(message);
    this.name = 'BusinessException';
  }
}
```

```typescript
// src/common/filters/business-exception.filter.ts
import { ExceptionFilter, Catch, ArgumentsHost } from '@nestjs/common';
import { Response } from 'express';
import { BusinessException } from '../exceptions/business.exception';

@Catch(BusinessException)
export class BusinessExceptionFilter implements ExceptionFilter<BusinessException> {
  catch(exception: BusinessException, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();

    response.status(exception.statusCode).json({
      success: false,
      error: {
        code: exception.code,
        message: exception.message,
        details: exception.details || null,
      },
    });
  }
}
```

```typescript
// src/common/filters/generic-exception.filter.ts
import { ExceptionFilter, Catch, ArgumentsHost, HttpStatus } from '@nestjs/common';
import { Response } from 'express';

@Catch()
export class GenericExceptionFilter implements ExceptionFilter {
  catch(exception: any, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();

    const status = exception.status || exception.statusCode || HttpStatus.INTERNAL_SERVER_ERROR;
    const message = exception.message || 'Internal server error';

    response.status(status).json({
      statusCode: status,
      message,
    });
  }
}
```

## Cara Integrasi & Pengujian

Implement `ExceptionFilter<T>` dengan method `catch`. `T` adalah exception type. Gunakan `@Catch(T)` untuk绑定 filter ke type tertentu.

**Test dengan curl:**
```bash
curl http://localhost:3000/business-test
# {"success":false,"error":{"code":"INSUFFICIENT_FUNDS","message":"Balance too low","details":null}}
```
