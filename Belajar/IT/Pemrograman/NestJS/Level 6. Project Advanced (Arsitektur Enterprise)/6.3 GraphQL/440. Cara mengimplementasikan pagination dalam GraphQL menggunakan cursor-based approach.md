# 440. Cara mengimplementasikan pagination dalam GraphQL menggunakan cursor-based approach

## Definisi & Target
Cursor-based pagination menggunakan cursor (biasanya ID atau timestamp) untuk menentukan posisi data. Lebih reliable untuk data real-time dibanding skip/take karena data baru tidak menggeser halaman.

## Komponen & Nest CLI Command
```
Relay Connection spec:
  edges: [Edge]        // Array data dengan cursor
  pageInfo: PageInfo   // Info: hasNextPage, hasPreviousPage, startCursor, endCursor
  totalCount: Int      // Total data (optional)
```

## Kode Implementasi (Per File)
```typescript
// src/common/models/pagination.model.ts
import { ObjectType, Field, Int } from '@nestjs/graphql';

@ObjectType()
export class PageInfo {
  @Field()
  hasNextPage: boolean;

  @Field()
  hasPreviousPage: boolean;

  @Field({ nullable: true })
  startCursor?: string;

  @Field({ nullable: true })
  endCursor?: string;
}

@ObjectType()
export class PaginationMeta {
  @Field(() => Int)
  totalCount: number;

  @Field(() => Int)
  totalPages: number;

  @Field(() => Int)
  currentPage: number;
}
```

```typescript
// src/posts/dto/posts-connection.model.ts
import { ObjectType, Field, Int, ID } from '@nestjs/graphql';
import { Post } from '../models/post.model';
import { PageInfo, PaginationMeta } from '../../common/models/pagination.model';

@ObjectType()
export class PostEdge {
  @Field()
  cursor: string;

  @Field(() => Post)
  node: Post;
}

@ObjectType()
export class PostsConnection {
  @Field(() => [PostEdge])
  edges: PostEdge[];

  @Field(() => PageInfo)
  pageInfo: PageInfo;

  @Field(() => PaginationMeta, { nullable: true })
  meta?: PaginationMeta;
}
```

```typescript
// src/posts/posts.resolver.ts (cursor pagination)
import { Resolver, Query, Args, Int } from '@nestjs/graphql';
import { PostsConnection } from './dto/posts-connection.model';
import { PostsService } from './posts.service';

@Resolver(() => Post)
export class PostsResolver {
  constructor(private readonly postsService: PostsService) {}

  @Query(() => PostsConnection)
  async postsConnection(
    @Args('first', { type: () => Int, nullable: true }) first?: number,
    @Args('after', { nullable: true }) after?: string,
    @Args('before', { nullable: true }) before?: string,
  ) {
    return this.postsService.findConnection({ first, after, before });
  }
}
```

## Cara Integrasi & Pengujian
Test pagination:
```graphql
query {
  postsConnection(first: 3, after: "cursor_1") {
    edges { cursor node { title } }
    pageInfo { hasNextPage endCursor }
  }
}
```
