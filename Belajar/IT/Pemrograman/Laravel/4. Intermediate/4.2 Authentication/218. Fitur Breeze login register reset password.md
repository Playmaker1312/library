# 218. Fitur Breeze Login Register Reset Password

## Definisi Singkat
Breeze menyediakan fitur autentikasi lengkap: login, register, logout, lupa password, reset password, verifikasi email, konfirmasi password, dan edit profil.

## Analogi
Seperti paket aplikasi kantor lengkap yang sudah termasuk Word, Excel, dan PowerPoint — semua fitur dasar yang Anda butuhkan sudah tersedia dalam satu paket.

## Poin-Poin Penting
- **Login**: autentikasi dengan email + password
- **Register**: buat akun baru (nama, email, password)
- **Logout**: hapus session dan redirect
- **Reset Password**: kirim link ke email, form set password baru
- **Email Verification**: kirim notifikasi verifikasi
- **Password Confirmation**: konfirmasi ulang untuk aksi sensitif
- **Profile**: edit nama, email, dan ganti password

## Contoh Kode
```php
<?php

// routes/auth.php — generated oleh Breeze
use App\Http\Controllers\Auth\AuthenticatedSessionController;
use App\Http\Controllers\Auth\RegisteredUserController;
use App\Http\Controllers\Auth\PasswordResetLinkController;
use App\Http\Controllers\Auth\NewPasswordController;
use App\Http\Controllers\Auth\EmailVerificationNotificationController;
use App\Http\Controllers\Auth\EmailVerificationPromptController;

Route::middleware('guest')->group(function () {
    Route::get('register', [RegisteredUserController::class, 'create'])->name('register');
    Route::post('register', [RegisteredUserController::class, 'store']);
    Route::get('login', [AuthenticatedSessionController::class, 'create'])->name('login');
    Route::post('login', [AuthenticatedSessionController::class, 'store']);
    Route::get('forgot-password', [PasswordResetLinkController::class, 'create'])->name('password.request');
    Route::post('forgot-password', [PasswordResetLinkController::class, 'store'])->name('password.email');
    Route::get('reset-password/{token}', [NewPasswordController::class, 'create'])->name('password.reset');
    Route::post('reset-password', [NewPasswordController::class, 'store'])->name('password.store');
});

Route::middleware('auth')->group(function () {
    Route::post('logout', [AuthenticatedSessionController::class, 'destroy'])->name('logout');
});
```

## Kasus Penggunaan Nyata
Aplikasi web standar yang memerlukan sistem registrasi dan login user, seperti forum, portal berita, e-commerce, atau dashboard admin.
