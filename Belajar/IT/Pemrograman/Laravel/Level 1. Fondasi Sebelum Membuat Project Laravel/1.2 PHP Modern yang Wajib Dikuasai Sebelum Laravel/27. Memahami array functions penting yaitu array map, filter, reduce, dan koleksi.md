# 27. Memahami Array Functions Penting yaitu Array Map, Filter, Reduce, dan Koleksi

## Definisi & Target
PHP memiliki fungsi array built-in dan Laravel memiliki Collection class yang lebih powerful. Target: mampu merubah, menyaring, dan mengolah data array/collection secara deklaratif.

## Komponen yang Terlibat
Controller, Service, atau class yang memproses data koleksi.

## Perintah Artisan CLI
Tidak ada.

## Kode Implementasi (Per File)
### PHP Native Array Functions
`php
 = [1, 2, 3, 4, 5];

 = array_map(fn() =>  * 2, );
 = array_filter(, fn() =>  % 2 === 0);
 = array_reduce(, fn(, ) =>  + , 0);
`

### Laravel Collection â€” lebih ekspresif
`php
 = Order::where('status', 'completed')->get();

 = 
    ->groupBy(fn(Order ) => ->created_at->format('Y-m'))
    ->map(function (, ) {
        return [
            'month' => ,
            'total' => ->sum('amount'),
            'count' => ->count(),
        ];
    })
    ->sortBy('month')
    ->values();
`

## Cara Integrasi & Pengujian
1. Laravel Collection menyediakan puluhan method: map, ilter, educe, groupBy, sortBy
2. Collection bersifat lazy â€” tidak mengeksekusi sampai dibutuhkan
3. Gunakan dd() atau dump() untuk debugging collection
4. Query Builder lebih efisien untuk data besar; Collection untuk transformasi
