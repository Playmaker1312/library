# 481. Inertia.js SPA Laravel

## Definisi Singkat
Framework yang menghubungkan Laravel dengan SPA frontend (React/Vue/Svelte) tanpa perlu API — menggunakan Laravel routing dan controller, frontend merender komponen.

## Analogi
Seperti jembatan antara dua gedung — Laravel (data/logika) di satu sisi, React/Vue (UI) di sisi lain. Inertia adalah jembatan yang menghubungkan tanpa perlu jalan memutar API.

## Poin-Poin Penting
- Tidak perlu API endpoint — pakai Laravel controller
- Component-based frontend (React/Vue/Svelte)
- Data dikirim sebagai JSON via Inertia
- No full page reload — SPA experience
- Back button bekerja normal (history)
- Validation errors, flash messages otomatis

## Contoh Kode
```php
<?php

// Install Inertia
composer require inertiajs/inertia-laravel
npm install @inertiajs/inertia @inertiajs/inertia-react

// app/Http/Kernel.php — tambah middleware
'web' => [
    // ...
    \App\Http\Middleware\HandleInertiaRequests::class,
],

// Controller — return Inertia response
namespace App\Http\Controllers;

use App\Models\Post;
use Inertia\Inertia;
use Inertia\Response;

class PostController extends Controller
{
    public function index(): Response
    {
        return Inertia::render('Posts/Index', [
            'posts' => Post::with('author')
                ->latest()
                ->paginate(10)
                ->through(fn ($post) => [
                    'id' => $post->id,
                    'title' => $post->title,
                    'excerpt' => Str::limit($post->content, 100),
                    'author' => $post->author->name,
                    'created_at' => $post->created_at->diffForHumans(),
                ]),
            'filters' => request()->only(['search', 'status']),
        ]);
    }

    public function show(Post $post): Response
    {
        return Inertia::render('Posts/Show', [
            'post' => [
                'id' => $post->id,
                'title' => $post->title,
                'content' => $post->content,
                'author' => $post->author->only('name', 'avatar'),
                'created_at' => $post->created_at->format('d M Y'),
            ],
        ]);
    }

    public function store(StorePostRequest $request): RedirectResponse
    {
        Post::create($request->validated());

        return redirect()->route('posts.index')
            ->with('message', 'Post created!');
    }
}

// routes/web.php
Route::resource('posts', PostController::class);
Route::get('/', function () {
    return Inertia::render('Welcome', [
        'appName' => config('app.name'),
    ]);
});

// Shared data — middleware
class HandleInertiaRequests extends Middleware
{
    public function share(Request $request): array
    {
        return [
            ...parent::share($request),
            'auth' => [
                'user' => $request->user()?->only('id', 'name', 'email'),
            ],
            'flash' => [
                'message' => fn () => $request->session()->get('message'),
            ],
        ];
    }
}
```

```jsx
// resources/js/Pages/Posts/Index.jsx
import { Head, Link, router } from '@inertiajs/react';

export default function PostsIndex({ posts, filters }) {
    const [search, setSearch] = useState(filters.search || '');

    // Search via Inertia (no API)
    function handleSearch(e) {
        setSearch(e.target.value);
        router.get('/posts', { search: e.target.value }, {
            preserveState: true,
            replace: true,
        });
    }

    return (
        <>
            <Head title="Posts" />
            <input value={search} onChange={handleSearch} placeholder="Search..." />
            {posts.data.map(post => (
                <div key={post.id}>
                    <Link href={`/posts/${post.id}`}>
                        <h3>{post.title}</h3>
                    </Link>
                    <p>{post.excerpt}</p>
                    <small>{post.author} - {post.created_at}</small>
                </div>
            ))}
        </>
    );
}
```

## Kasus Penggunaan Nyata
Dashboard analytics — Inertia + React memberikan SPA experience (navigasi mulus tanpa load ulang) tapi tetap pakai Laravel controller dan routing yang sudah ada.
