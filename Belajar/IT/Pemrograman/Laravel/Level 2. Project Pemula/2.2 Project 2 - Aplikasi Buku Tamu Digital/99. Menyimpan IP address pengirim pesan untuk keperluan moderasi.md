# 99. Menyimpan IP address pengirim pesan untuk keperluan moderasi

## Definisi & Target
Menyimpan alamat IP pengirim saat mengirim pesan buku tamu. IP address membantu admin melakukan moderasi dengan mendeteksi pola spam dari IP yang sama.

## Komponen yang Terlibat
- Migration (sudah ada kolom `ip_address`)
- GuestbookController (method store)

## Perintah Artisan CLI
Tidak ada perintah Artisan.

## Kode Implementasi (Per File)

**GuestbookController (method store - simpan IP)**
```php
public function store(Request $request)
{
    $validated = $request->validate([...]);

    if ($request->filled('website')) {
        return back()->with('error', 'Terdeteksi sebagai spam.');
    }

    GuestbookEntry::create([
        'name' => $validated['name'],
        'email' => $validated['email'],
        'message' => $validated['message'],
        'status' => GuestbookEntry::STATUS_PENDING,
        'ip_address' => $request->ip(),
    ]);

    Mail::to(config('mail.admin_address'))->send(new NewGuestbookEntry($entry));

    return redirect()->route('guestbook.index')
        ->with('success', 'Pesan Anda telah dikirim dan menunggu moderasi.');
}
```

**Tampilkan IP di halaman admin (admin/guestbook/index.blade.php)**
```php
<td class="py-3 px-4 text-xs">{{ $entry->ip_address ?? '-' }}</td>
```

## Cara Integrasi & Pengujian
1. Kirim pesan dari form buku tamu
2. Cek di halaman admin - kolom IP address akan menampilkan IP pengirim
3. Pastikan IP address tersimpan dengan benar di database
