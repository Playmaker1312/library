# 139. Mengimplementasikan fitur pencarian dan filter produk yang komprehensif

## Definisi & Target
Membuat fitur pencarian dan filter yang komprehensif untuk produk. Pengguna dapat mencari berdasarkan nama, SKU, atau barcode, serta memfilter berdasarkan kategori dan status stok.

## Komponen yang Terlibat
- ProductController (method index)
- Blade View: `resources/views/products/index.blade.php`

## Perintah Artisan CLI
Tidak ada perintah Artisan.

## Kode Implementasi (Per File)

**ProductController (method index dengan filter lengkap)**
```php
public function index(Request $request)
{
    $query = Product::with('category');

    if ($request->filled('search')) {
        $search = $request->search;
        $query->where(function ($q) use ($search) {
            $q->where('name', 'like', "%{$search}%")
                ->orWhere('sku', 'like', "%{$search}%")
                ->orWhere('barcode', 'like', "%{$search}%");
        });
    }

    if ($request->filled('category')) {
        $query->where('category_id', $request->category);
    }

    if ($request->filled('stock_status')) {
        switch ($request->stock_status) {
            case 'available':
                $query->where('stock', '>', 0);
                break;
            case 'low':
                $query->lowStock()->where('stock', '>', 0);
                break;
            case 'out':
                $query->outOfStock();
                break;
        }
    }

    $sortField = $request->get('sort', 'created_at');
    $sortDirection = $request->get('direction', 'desc');
    $allowedSorts = ['name', 'stock', 'created_at', 'selling_price'];
    if (in_array($sortField, $allowedSorts)) {
        $query->orderBy($sortField, $sortDirection === 'asc' ? 'asc' : 'desc');
    }

    $products = $query->paginate(12)->withQueryString();
    $categories = Category::all();

    return view('products.index', compact('products', 'categories'));
}
```

**resources/views/products/index.blade.php (form filter)**
```php
<form action="{{ route('products.index') }}" method="GET" class="bg-white rounded-lg shadow-md p-4 mb-6">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
        <div>
            <input type="text" name="search" value="{{ request('search') }}"
                placeholder="Cari nama, SKU, atau barcode..."
                class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400">
        </div>
        <div>
            <select name="category" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400">
                <option value="">Semua Kategori</option>
                @foreach($categories as $category)
                    <option value="{{ $category->id }}" {{ request('category') == $category->id ? 'selected' : '' }}>
                        {{ $category->name }}
                    </option>
                @endforeach
            </select>
        </div>
        <div>
            <select name="stock_status" class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400">
                <option value="">Semua Status Stok</option>
                <option value="available" {{ request('stock_status') == 'available' ? 'selected' : '' }}>Tersedia</option>
                <option value="low" {{ request('stock_status') == 'low' ? 'selected' : '' }}>Stok Menipis</option>
                <option value="out" {{ request('stock_status') == 'out' ? 'selected' : '' }}>Stok Habis</option>
            </select>
        </div>
        <div class="flex gap-2">
            <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg flex-1">
                Filter
            </button>
            <a href="{{ route('products.index') }}" class="bg-gray-300 hover:bg-gray-400 text-gray-700 px-4 py-2 rounded-lg">
                Reset
            </a>
        </div>
    </div>

    <div class="mt-3 flex items-center gap-2 text-sm">
        <span>Urutkan:</span>
        <a href="{{ request()->fullUrlWithQuery(['sort' => 'name', 'direction' => request('sort') == 'name' && request('direction') == 'asc' ? 'desc' : 'asc']) }}"
            class="text-blue-500 hover:underline {{ request('sort') == 'name' ? 'font-bold' : '' }}">
            Nama @if(request('sort') == 'name') {{ request('direction') == 'asc' ? '↑' : '↓' }} @endif
        </a>
        <a href="{{ request()->fullUrlWithQuery(['sort' => 'stock', 'direction' => request('sort') == 'stock' && request('direction') == 'asc' ? 'desc' : 'asc']) }}"
            class="text-blue-500 hover:underline {{ request('sort') == 'stock' ? 'font-bold' : '' }}">
            Stok @if(request('sort') == 'stock') {{ request('direction') == 'asc' ? '↑' : '↓' }} @endif
        </a>
        <a href="{{ request()->fullUrlWithQuery(['sort' => 'selling_price', 'direction' => request('sort') == 'selling_price' && request('direction') == 'asc' ? 'desc' : 'asc']) }}"
            class="text-blue-500 hover:underline {{ request('sort') == 'selling_price' ? 'font-bold' : '' }}">
            Harga @if(request('sort') == 'selling_price') {{ request('direction') == 'asc' ? '↑' : '↓' }} @endif
        </a>
    </div>
</form>
```

## Cara Integrasi & Pengujian
1. Buka halaman daftar produk
2. Gunakan berbagai filter: pencarian, kategori, status stok
3. Urutkan berdasarkan nama, stok, atau harga
4. Kombinasikan filter untuk hasil yang spesifik
