# 344. Membangun Modul Manajemen Siswa dengan Data Lengkap dan Foto

## Definisi & Target
CRUD manajemen siswa dengan data lengkap (nis, nama, alamat, orang tua, foto) dan upload foto. Target: modul admin untuk mengelola data siswa.

## Komponen yang Terlibat
- StudentController (resource)
- Student model
- File upload foto
- Filter kelas & status
- DataTables atau pagination

## Perintah Artisan CLI
```bash
php artisan make:controller StudentController --resource
php artisan make:livewire StudentForm
```

## Kode Implementasi (Per File)

### Controller
```php
// app/Http/Controllers/StudentController.php
namespace App\Http\Controllers;

use App\Models\Classroom;
use App\Models\Student;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Storage;

class StudentController extends Controller
{
    public function index(Request $request)
    {
        $query = Student::with('classroom');

        if ($request->filled('search')) {
            $query->where(function ($q) use ($request) {
                $q->where('name', 'like', "%{$request->search}%")
                  ->orWhere('nis', 'like', "%{$request->search}%")
                  ->orWhere('nisn', 'like', "%{$request->search}%");
            });
        }

        if ($request->filled('class_id')) {
            $query->where('class_id', $request->class_id);
        }

        if ($request->filled('status')) {
            $query->where('status', $request->status);
        }

        $students = $query->latest()->paginate(20);
        $classes = Classroom::all();

        return view('students.index', compact('students', 'classes'));
    }

    public function create()
    {
        $classes = Classroom::all();
        return view('students.create', compact('classes'));
    }

    public function store(Request $request)
    {
        $validated = $request->validate([
            'nis' => 'required|unique:students,nis|max:20',
            'nisn' => 'nullable|unique:students,nisn|max:20',
            'name' => 'required|max:255',
            'gender' => 'required|in:L,P',
            'birth_place' => 'nullable|max:100',
            'birth_date' => 'nullable|date',
            'address' => 'nullable|max:1000',
            'phone' => 'nullable|max:20',
            'photo' => 'nullable|image|max:2048',
            'parent_name' => 'nullable|max:255',
            'parent_phone' => 'nullable|max:20',
            'parent_occupation' => 'nullable|max:100',
            'class_id' => 'nullable|exists:classes,id',
            'entry_year' => 'required|integer|min:2000|max:' . (date('Y') + 1),
        ]);

        if ($request->hasFile('photo')) {
            $validated['photo'] = $request->file('photo')->store('students', 'public');
        }

        Student::create($validated);

        return redirect()->route('students.index')
            ->with('success', 'Siswa berhasil ditambahkan.');
    }

    public function show(Student $student)
    {
        $student->load(['classroom', 'grades.subject']);
        return view('students.show', compact('student'));
    }

    public function edit(Student $student)
    {
        $classes = Classroom::all();
        return view('students.edit', compact('student', 'classes'));
    }

    public function update(Request $request, Student $student)
    {
        $validated = $request->validate([
            'nis' => 'required|max:20|unique:students,nis,' . $student->id,
            'nisn' => 'nullable|max:20|unique:students,nisn,' . $student->id,
            'name' => 'required|max:255',
            'gender' => 'required|in:L,P',
            'birth_place' => 'nullable|max:100',
            'birth_date' => 'nullable|date',
            'address' => 'nullable|max:1000',
            'phone' => 'nullable|max:20',
            'photo' => 'nullable|image|max:2048',
            'parent_name' => 'nullable|max:255',
            'parent_phone' => 'nullable|max:20',
            'parent_occupation' => 'nullable|max:100',
            'class_id' => 'nullable|exists:classes,id',
            'entry_year' => 'required|integer|min:2000|max:' . (date('Y') + 1),
            'status' => 'required|in:active,alumni,dropped',
        ]);

        if ($request->hasFile('photo')) {
            if ($student->photo) Storage::disk('public')->delete($student->photo);
            $validated['photo'] = $request->file('photo')->store('students', 'public');
        }

        $student->update($validated);

        return redirect()->route('students.index')
            ->with('success', 'Data siswa diperbarui.');
    }

    public function destroy(Student $student)
    {
        if ($student->photo) Storage::disk('public')->delete($student->photo);
        $student->delete();

        return redirect()->route('students.index')
            ->with('success', 'Siswa berhasil dihapus.');
    }
}
```

### Routes
```php
// routes/web.php
use App\Http\Controllers\StudentController;

Route::middleware(['auth', 'role:admin|teacher'])->group(function () {
    Route::resource('students', StudentController::class);
});
```

### Blade Views
```blade
{{-- resources/views/students/index.blade.php --}}
<x-app-layout>
    <x-slot name="title">Manajemen Siswa</x-slot>

    <div class="container py-8">
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-2xl font-bold">Data Siswa</h1>
            <a href="{{ route('students.create') }}" class="btn btn-primary">Tambah Siswa</a>
        </div>

        <div class="bg-white p-4 rounded shadow mb-6">
            <form class="flex gap-4 items-end">
                <div class="flex-1">
                    <input type="text" name="search" placeholder="Cari nama/NIS/NISN..."
                           value="{{ request('search') }}" class="w-full border rounded p-2">
                </div>
                <div>
                    <select name="class_id" class="border rounded p-2">
                        <option value="">Semua Kelas</option>
                        @foreach ($classes as $class)
                            <option value="{{ $class->id }}" {{ request('class_id') == $class->id ? 'selected' : '' }}>
                                {{ $class->level }} - {{ $class->name }}
                            </option>
                        @endforeach
                    </select>
                </div>
                <div>
                    <select name="status" class="border rounded p-2">
                        <option value="">Semua Status</option>
                        <option value="active" {{ request('status') === 'active' ? 'selected' : '' }}>Aktif</option>
                        <option value="alumni" {{ request('status') === 'alumni' ? 'selected' : '' }}>Alumni</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary">Cari</button>
            </form>
        </div>

        <div class="bg-white rounded shadow overflow-hidden">
            <table class="w-full">
                <thead>
                    <tr class="bg-gray-50 text-left">
                        <th class="p-3">NIS</th>
                        <th class="p-3">Nama</th>
                        <th class="p-3">Kelas</th>
                        <th class="p-3">JK</th>
                        <th class="p-3">Status</th>
                        <th class="p-3">Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach ($students as $student)
                        <tr class="border-t">
                            <td class="p-3">{{ $student->nis }}</td>
                            <td class="p-3 flex items-center gap-3">
                                @if ($student->photo)
                                    <img src="{{ asset('storage/' . $student->photo) }}" class="w-8 h-8 rounded-full object-cover">
                                @endif
                                {{ $student->name }}
                            </td>
                            <td class="p-3">{{ $student->classroom?->level }} {{ $student->classroom?->name ?? '-' }}</td>
                            <td class="p-3">{{ $student->gender === 'L' ? 'Laki-laki' : 'Perempuan' }}</td>
                            <td class="p-3">
                                <span class="px-2 py-1 rounded text-xs {{ $student->status === 'active' ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-700' }}">
                                    {{ $student->status === 'active' ? 'Aktif' : 'Alumni' }}
                                </span>
                            </td>
                            <td class="p-3 flex gap-2">
                                <a href="{{ route('students.show', $student) }}" class="text-blue-600 text-sm">Detail</a>
                                <a href="{{ route('students.edit', $student) }}" class="text-yellow-600 text-sm">Edit</a>
                                <form action="{{ route('students.destroy', $student) }}" method="POST"
                                      onsubmit="return confirm('Hapus siswa ini?')">
                                    @csrf @method('DELETE')
                                    <button type="submit" class="text-red-600 text-sm">Hapus</button>
                                </form>
                            </td>
                        </tr>
                    @endforeach
                </tbody>
            </table>
            <div class="p-4">{{ $students->links() }}</div>
        </div>
    </div>
</x-app-layout>
```

```blade
{{-- resources/views/students/create.blade.php --}}
<x-app-layout>
    <x-slot name="title">Tambah Siswa</x-slot>

    <div class="container py-8 max-w-3xl">
        <h1 class="text-2xl font-bold mb-6">Tambah Siswa Baru</h1>

        <form action="{{ route('students.store') }}" method="POST" enctype="multipart/form-data"
              class="bg-white p-6 rounded shadow space-y-4">
            @csrf

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block font-medium mb-1">NIS *</label>
                    <input type="text" name="nis" class="w-full border rounded p-2" required>
                </div>
                <div>
                    <label class="block font-medium mb-1">NISN</label>
                    <input type="text" name="nisn" class="w-full border rounded p-2">
                </div>
            </div>

            <div>
                <label class="block font-medium mb-1">Nama Lengkap *</label>
                <input type="text" name="name" class="w-full border rounded p-2" required>
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block font-medium mb-1">Jenis Kelamin</label>
                    <select name="gender" class="w-full border rounded p-2" required>
                        <option value="L">Laki-laki</option>
                        <option value="P">Perempuan</option>
                    </select>
                </div>
                <div>
                    <label class="block font-medium mb-1">Tahun Masuk</label>
                    <input type="number" name="entry_year" value="{{ date('Y') }}"
                           class="w-full border rounded p-2" required>
                </div>
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block font-medium mb-1">Tempat Lahir</label>
                    <input type="text" name="birth_place" class="w-full border rounded p-2">
                </div>
                <div>
                    <label class="block font-medium mb-1">Tanggal Lahir</label>
                    <input type="date" name="birth_date" class="w-full border rounded p-2">
                </div>
            </div>

            <div>
                <label class="block font-medium mb-1">Alamat</label>
                <textarea name="address" rows="3" class="w-full border rounded p-2"></textarea>
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block font-medium mb-1">No. Telepon</label>
                    <input type="text" name="phone" class="w-full border rounded p-2">
                </div>
                <div>
                    <label class="block font-medium mb-1">Kelas</label>
                    <select name="class_id" class="w-full border rounded p-2">
                        <option value="">Pilih Kelas</option>
                        @foreach ($classes as $class)
                            <option value="{{ $class->id }}">{{ $class->level }} {{ $class->name }}</option>
                        @endforeach
                    </select>
                </div>
            </div>

            <div>
                <label class="block font-medium mb-1">Foto</label>
                <input type="file" name="photo" accept="image/*" class="w-full">
            </div>

            <hr class="my-4">

            <h3 class="font-bold">Data Orang Tua</h3>
            <div class="grid grid-cols-3 gap-4">
                <div>
                    <label class="block font-medium mb-1">Nama Ayah/Ibu</label>
                    <input type="text" name="parent_name" class="w-full border rounded p-2">
                </div>
                <div>
                    <label class="block font-medium mb-1">No. Telepon</label>
                    <input type="text" name="parent_phone" class="w-full border rounded p-2">
                </div>
                <div>
                    <label class="block font-medium mb-1">Pekerjaan</label>
                    <input type="text" name="parent_occupation" class="w-full border rounded p-2">
                </div>
            </div>

            <button type="submit" class="btn btn-primary w-full mt-4">Simpan</button>
        </form>
    </div>
</x-app-layout>
```

## Cara Integrasi & Pengujian
1. Akses `/students`, lihat daftar siswa
2. Filter berdasarkan kelas dan status
3. Tambah siswa baru dengan data lengkap dan foto
4. Edit data siswa, ganti foto
5. Lihat detail siswa dengan grade
6. Hapus siswa (soft cascade)
7. Pastikan validasi NIS unique berfungsi
