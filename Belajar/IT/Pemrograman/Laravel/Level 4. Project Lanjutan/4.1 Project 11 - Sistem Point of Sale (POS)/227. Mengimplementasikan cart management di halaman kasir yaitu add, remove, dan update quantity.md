# 227. Mengimplementasikan Cart Management di Halaman Kasir yaitu Add, Remove, dan Update Quantity

## Definisi & Target
Membangun fungsionalitas cart management pada halaman kasir: menambah produk ke keranjang, menghapus item, dan mengubah quantity. Data cart disimpan dalam properti array di komponen Livewire.

## Komponen yang Terlibat
- Livewire Component: `KasirPage`
- Session (opsional untuk persistensi cart)

## Perintah Artisan CLI
- Tidak ada perintah khusus

## Kode Implementasi (Per File)

**app/Livewire/KasirPage.php (cart methods):**
```php
class KasirPage extends Component
{
    public $cart = [];

    public function addToCart($productId)
    {
        $product = Product::findOrFail($productId);

        $existingIndex = collect($this->cart)->search(fn($item) => $item['product_id'] === $productId);

        if ($existingIndex !== false) {
            $this->cart[$existingIndex]['quantity']++;
            $this->cart[$existingIndex]['subtotal'] = $this->cart[$existingIndex]['quantity'] * $this->cart[$existingIndex]['price'];
        } else {
            $this->cart[] = [
                'product_id' => $product->id,
                'name' => $product->name,
                'price' => $product->price,
                'quantity' => 1,
                'subtotal' => $product->price,
            ];
        }

        $this->calculateTotals();
    }

    public function updateQuantity($index, $action)
    {
        if (!isset($this->cart[$index])) return;

        if ($action === 'increase') {
            $this->cart[$index]['quantity']++;
        } elseif ($action === 'decrease') {
            $this->cart[$index]['quantity']--;
            if ($this->cart[$index]['quantity'] < 1) {
                $this->cart[$index]['quantity'] = 1;
            }
        }

        $this->cart[$index]['subtotal'] = $this->cart[$index]['quantity'] * $this->cart[$index]['price'];
        $this->calculateTotals();
    }

    public function removeFromCart($index)
    {
        unset($this->cart[$index]);
        $this->cart = array_values($this->cart);
        $this->calculateTotals();
    }

    public function calculateTotals()
    {
        $this->subtotal = collect($this->cart)->sum('subtotal');
        $this->tax = $this->subtotal * 0.11;
        $this->total = $this->subtotal + $this->tax - $this->discount;
    }
}
```

**resources/views/livewire/kasir-page.blade.php (cart section):**
```html
<div class="w-96 bg-white shadow-lg p-4 flex flex-col">
    <div class="flex justify-between items-center mb-4">
        <h2 class="text-xl font-bold">Keranjang</h2>
        <span class="bg-blue-600 text-white px-2 py-1 rounded-full text-sm">{{ count($cart) }} item</span>
    </div>

    <div class="flex-1 overflow-y-auto space-y-2">
        @forelse($cart as $index => $item)
        <div class="flex items-center justify-between p-3 bg-gray-50 rounded-lg" wire:key="cart-{{ $index }}">
            <div class="flex-1">
                <p class="font-semibold text-sm">{{ $item['name'] }}</p>
                <p class="text-xs text-gray-500">Rp {{ number_format($item['price'], 0, ',', '.') }}</p>
            </div>
            <div class="flex items-center space-x-2">
                <button wire:click="updateQuantity({{ $index }}, 'decrease')"
                        class="w-7 h-7 bg-gray-200 rounded-full flex items-center justify-center hover:bg-gray-300">-</button>
                <span class="w-6 text-center font-semibold">{{ $item['quantity'] }}</span>
                <button wire:click="updateQuantity({{ $index }}, 'increase')"
                        class="w-7 h-7 bg-gray-200 rounded-full flex items-center justify-center hover:bg-gray-300">+</button>
            </div>
            <div class="text-right ml-3">
                <p class="font-semibold">Rp {{ number_format($item['subtotal'], 0, ',', '.') }}</p>
                <button wire:click="removeFromCart({{ $index }})" class="text-red-500 text-xs hover:underline">Hapus</button>
            </div>
        </div>
        @empty
        <div class="text-center py-10 text-gray-400">
            <p class="text-4xl mb-2">🛒</p>
            <p>Belum ada item</p>
        </div>
        @endforelse
    </div>
</div>
```

## Cara Integrasi & Pengujian
- Klik produk untuk menambah ke keranjang
- Jika produk sama sudah ada, quantity bertambah
- Klik tombol +/- untuk mengubah quantity
- Klik "Hapus" untuk menghapus item dari cart
- Verifikasi total perhitungan berubah setiap kali cart dimodifikasi
