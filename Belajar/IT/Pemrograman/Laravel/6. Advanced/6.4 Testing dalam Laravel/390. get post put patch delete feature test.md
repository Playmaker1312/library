# 390. get post put patch delete feature test

## Definisi Singkat
Feature test menggunakan method HTTP seperti `get()`, `post()`, `put()`, `patch()`, `delete()` untuk mensimulasikan request ke aplikasi.

## Analogi
Seperti test driver mobil — Anda simulasi berbagai kondisi (gas, rem, belok) tanpa benar-benar keluar jalan raya.

## Poin-Poin Penting
- `$this->get($uri)` — simulasi GET request
- `$this->post($uri, $data)` — simulasi POST dengan data
- `$this->put($uri, $data)` — simulasi PUT (update penuh)
- `$this->patch($uri, $data)` — simulasi PATCH (update sebagian)
- `$this->delete($uri)` — simulasi DELETE
- Semua return `Illuminate\Testing\TestResponse`

## Contoh Kode
```php
<?php

namespace Tests\Feature\Http\Controllers;

use Tests\TestCase;
use App\Models\Product;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;

class ProductControllerTest extends TestCase
{
    use RefreshDatabase;

    private User $admin;

    protected function setUp(): void
    {
        parent::setUp();
        $this->admin = User::factory()->create(['role' => 'admin']);
    }

    // GET — lihat daftar produk
    public function test_can_list_products(): void
    {
        Product::factory()->count(3)->create();

        $response = $this->actingAs($this->admin)
            ->get('/products');

        $response->assertStatus(200);
        $response->assertViewHas('products');
    }

    // GET — lihat detail produk
    public function test_can_show_product(): void
    {
        $product = Product::factory()->create();

        $response = $this->actingAs($this->admin)
            ->get("/products/{$product->id}");

        $response->assertStatus(200);
        $response->assertSee($product->name);
    }

    // POST — buat produk baru
    public function test_can_create_product(): void
    {
        $data = [
            'name' => 'Laptop Baru',
            'price' => 15000000,
            'stock' => 10,
        ];

        $response = $this->actingAs($this->admin)
            ->post('/products', $data);

        $response->assertStatus(302); // Redirect
        $this->assertDatabaseHas('products', $data);
    }

    // PUT — update seluruh data produk
    public function test_can_update_product(): void
    {
        $product = Product::factory()->create();

        $response = $this->actingAs($this->admin)
            ->put("/products/{$product->id}", [
                'name' => 'Laptop Updated',
                'price' => 16000000,
                'stock' => 5,
            ]);

        $response->assertStatus(302);
        $this->assertDatabaseHas('products', [
            'id' => $product->id,
            'name' => 'Laptop Updated',
        ]);
    }

    // PATCH — update sebagian data
    public function test_can_partially_update_product(): void
    {
        $product = Product::factory()->create(['price' => 10000]);

        $response = $this->actingAs($this->admin)
            ->patch("/products/{$product->id}", [
                'price' => 12000,
            ]);

        $response->assertStatus(302);
        $this->assertEquals(12000, $product->fresh()->price);
    }

    // DELETE — hapus produk
    public function test_can_delete_product(): void
    {
        $product = Product::factory()->create();

        $response = $this->actingAs($this->admin)
            ->delete("/products/{$product->id}");

        $response->assertStatus(302);
        $this->assertModelMissing($product);
    }
}
```

```php
<?php
// Helper testing untuk berbagai request

// JSON API
$this->postJson('/api/products', $data);
$this->getJson('/api/products');
$this->putJson('/api/products/1', $data);
$this->deleteJson('/api/products/1');

// Dengan headers
$this->withHeaders([
    'Authorization' => 'Bearer token',
])->post('/api/products', $data);

// Dengan session
$this->withSession(['role' => 'admin'])
    ->get('/dashboard');
```

## Kasus Penggunaan Nyata
Menguji semua endpoint API, CRUD controller, autentikasi, validasi form, dan memastikan routing bekerja dengan benar.
