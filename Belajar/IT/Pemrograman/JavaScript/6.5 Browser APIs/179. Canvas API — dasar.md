### Definisi Singkat
**Canvas API** memungkinkan rendering grafik 2D secara dinamis melalui elemen HTML `<canvas>`. Dengan JavaScript, kita bisa menggambar bentuk, garis, teks, gambar, animasi, dan efek visual langsung di browser.

---

### Analogi
Canvas seperti **kanvas lukis digital** dan **kuas pintar**. Kamu punya kanvas (`<canvas>`), kuas (`context`), dan berbagai cat (`method API`) untuk menggambar garis, lingkaran, kotak, dan teks.

---

### Poin-Poin Penting
- Elemen HTML: `<canvas id="c" width="500" height="300"></canvas>`
- Dapatkan context: `canvas.getContext('2d')`
- Koordinat dimulai dari kiri atas (0,0)
- Drawing method: `fillRect`, `strokeRect`, `arc`, `beginPath`, `moveTo`, `lineTo`, `fill`, `stroke`
- Style: `fillStyle`, `strokeStyle`, `lineWidth`, `font`, `shadow*`
- Animasi menggunakan `requestAnimationFrame`
- Canvas bersifat pixel-based (bukan vector)

---

### Contoh Implementasi

```javascript
const canvas = document.getElementById('myCanvas');
const ctx = canvas.getContext('2d');

// Kotak
ctx.fillStyle = 'red';
ctx.fillRect(10, 10, 100, 50);
ctx.strokeStyle = 'blue';
ctx.lineWidth = 3;
ctx.strokeRect(130, 10, 100, 50);

// Lingkaran
ctx.beginPath();
ctx.arc(300, 35, 30, 0, Math.PI * 2);
ctx.fillStyle = 'green';
ctx.fill();

// Garis
ctx.beginPath();
ctx.moveTo(10, 100);
ctx.lineTo(200, 200);
ctx.strokeStyle = 'purple';
ctx.lineWidth = 5;
ctx.stroke();

// Teks
ctx.font = '20px Arial';
ctx.fillStyle = 'black';
ctx.fillText('Halo Canvas!', 10, 250);

// Animasi sederhana
let x = 0;
function animate() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  ctx.fillStyle = 'orange';
  ctx.beginPath();
  ctx.arc(x++, 150, 20, 0, Math.PI * 2);
  ctx.fill();
  requestAnimationFrame(animate);
}
animate();
```

---

### Koneksi
- **156 →** Canvas adalah elemen HTML yang bisa dimanipulasi via DOM
- **182 →** ResizeObserver untuk merespons perubahan ukuran canvas
- **181 →** IntersectionObserver untuk mulai/hentikan animasi canvas