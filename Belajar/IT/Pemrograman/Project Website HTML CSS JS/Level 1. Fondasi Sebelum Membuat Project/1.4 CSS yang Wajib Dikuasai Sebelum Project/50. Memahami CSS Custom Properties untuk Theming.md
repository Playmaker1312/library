# 50. Memahami CSS Custom Properties untuk Theming

## Definisi & Target
CSS Custom Properties (CSS variables) adalah nilai yang didefinisikan sekali dan bisa digunakan ulang di seluruh CSS. Sangat berguna untuk theming (light/dark mode).

## Analogi Alur
CSS variables seperti panel kontrol pencahayaan rumah. Dengan satu saklar (toggle theme), semua lampu di rumah berubah warna. Anda tidak perlu mengganti bola lampu satu per satu.

## Poin-Poin Penting
- Definisi: `--nama-variabel: nilai;` di `:root` atau selector
- Penggunaan: `property: var(--nama-variabel, fallback);`
- Bersifat inheritable (diwariskan ke child elements)
- Bisa diubah via JavaScript: `element.style.setProperty('--nama', 'nilai')`
- Sangat berguna untuk dark mode, theming multisite
- Media query `prefers-color-scheme` untuk dark mode otomatis

## Kode Implementasi (Gabungan) - HTML, CSS, JavaScript
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CSS Theming Demo</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body class="light-theme">
  <div class="theme-demo">
    <h1>CSS Theming dengan Custom Properties</h1>
    <p>Klik tombol untuk toggle Light/Dark mode.</p>
    <button id="btnTheme">Toggle Dark Mode</button>
    <div class="theme-info">
      <p>Current theme: <span id="themeLabel">Light</span></p>
    </div>
    <div class="card-theme">
      <h2>Card dengan Theming</h2>
      <p>Ini adalah card yang warnanya mengikuti tema aktif. Background, text, dan border semua menggunakan CSS variables.</p>
      <button class="btn-primary">Tombol</button>
    </div>
  </div>
  <script src="js/script.js"></script>
</body>
</html>
```
```css
:root {
  --bg-primary: #ffffff;
  --bg-secondary: #f8f9fa;
  --text-primary: #2c3e50;
  --text-secondary: #666;
  --accent: #3498db;
  --accent-hover: #2980b9;
  --border: #e0e0e0;
  --shadow: 0 4px 20px rgba(0,0,0,0.1);
  --card-bg: #ffffff;
}
.dark-theme {
  --bg-primary: #1a1a2e;
  --bg-secondary: #16213e;
  --text-primary: #e0e0e0;
  --text-secondary: #a0a0a0;
  --accent: #64b5f6;
  --accent-hover: #90caf9;
  --border: #333;
  --shadow: 0 4px 20px rgba(0,0,0,0.3);
  --card-bg: #16213e;
}
body { font-family: Arial, sans-serif; margin: 0; padding: 20px; background: var(--bg-primary); color: var(--text-primary); transition: all 0.3s; }
.theme-demo { max-width: 600px; margin: 40px auto; background: var(--card-bg); padding: 40px; border-radius: 12px; box-shadow: var(--shadow); border: 1px solid var(--border); }
.theme-info { background: var(--bg-secondary); padding: 15px; border-radius: 8px; margin: 20px 0; }
.card-theme { background: var(--bg-secondary); padding: 20px; border-radius: 8px; border: 1px solid var(--border); }
.card-theme h2 { color: var(--text-primary); }
.card-theme p { color: var(--text-secondary); }
.btn-primary, #btnTheme { padding: 10px 20px; border: none; border-radius: 6px; background: var(--accent); color: white; cursor: pointer; }
.btn-primary:hover, #btnTheme:hover { background: var(--accent-hover); }
#themeLabel { font-weight: bold; color: var(--accent); }
```
```javascript
var btnTheme = document.getElementById('btnTheme');
var themeLabel = document.getElementById('themeLabel');
var body = document.body;

btnTheme.addEventListener('click', function() {
  body.classList.toggle('dark-theme');
  var isDark = body.classList.contains('dark-theme');
  themeLabel.textContent = isDark ? 'Dark' : 'Light';
  btnTheme.textContent = isDark ? 'Toggle Light Mode' : 'Toggle Dark Mode';
});
```

## Cara Integrasi
1. Definisikan semua variabel warna di `:root` untuk default (light)
2. Buat class `.dark-theme` atau `[data-theme="dark"]` yang override variabel
3. Gunakan `var(--nama)` di seluruh CSS (jangan hardcode warna)
4. Toggle class dengan JavaScript untuk ganti tema
5. Tambahkan `prefers-color-scheme` untuk dark mode otomatis berdasarkan sistem
