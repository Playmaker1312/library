# 20. Cara Menghubungkan File CSS Eksternal ke HTML

## Definisi & Target
CSS eksternal adalah file .css terpisah yang dihubungkan ke HTML menggunakan tag `<link>`. Metode ini memisahkan konten (HTML) dari presentasi (CSS).

## Analogi Alur
HTML adalah kerangka rumah, CSS adalah cat dan dekorasi. Dengan CSS eksternal, Anda punya satu buku panduan dekorasi yang bisa diterapkan ke banyak ruangan (halaman HTML) sekaligus.

## Poin-Poin Penting
- Gunakan `<link rel="stylesheet" href="path/file.css">` di `<head>`
- Path bisa relatif atau absolut
- Satu file CSS bisa dipakai banyak halaman HTML (reusable)
- Multiple CSS files bisa di-link sekaligus
- Cache browser menyimpan CSS untuk load lebih cepat

## Kode Implementasi (Gabungan) - HTML, CSS, JavaScript
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CSS Eksternal</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <div class="card">
    <h2 class="card-title">CSS Eksternal</h2>
    <p class="card-text">Style ini berasal dari file CSS eksternal.</p>
    <button class="btn">Tombol</button>
  </div>
</body>
</html>
```
```css
body {
  font-family: Arial, sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  margin: 0;
  background: #f0f2f5;
}
.card {
  background: white;
  padding: 30px;
  border-radius: 12px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  text-align: center;
  max-width: 400px;
}
.card-title {
  color: #2c3e50;
  margin-bottom: 10px;
}
.card-text {
  color: #7f8c8d;
  line-height: 1.6;
}
.btn {
  background: #3498db;
  color: white;
  border: none;
  padding: 10px 24px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 16px;
}
.btn:hover {
  background: #2980b9;
}
```

## Cara Integrasi
1. Buat file `css/style.css`
2. Di HTML, tambahkan `<link rel="stylesheet" href="css/style.css">` di `<head>`
3. Semua style di file CSS akan otomatis diterapkan ke HTML
4. Untuk multiple CSS: tambahkan multiple `<link>` tags
5. Pastikan path file CSS benar (relatif terhadap file HTML)
