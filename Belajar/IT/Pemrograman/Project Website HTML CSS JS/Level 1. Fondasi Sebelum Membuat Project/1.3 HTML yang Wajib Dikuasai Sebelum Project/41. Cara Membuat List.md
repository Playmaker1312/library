# 41. Cara Membuat List Menggunakan Ul, Ol, dan Dl

## Definisi & Target
HTML menyediakan tiga jenis list: `<ul>` (unordered/bullet), `<ol>` (ordered/numbered), dan `<dl>` (definition list). Masing-masing untuk jenis data berbeda.

## Analogi Alur
- `ul` (unordered list): seperti daftar belanja - urutan tidak penting
- `ol` (ordered list): seperti resep masakan - urutan langkah penting
- `dl` (definition list): seperti kamus - pasangan istilah dan definisi

## Poin-Poin Penting
- `<ul>` + `<li>`: daftar dengan bullet (circle, disc, square via CSS)
- `<ol>` + `<li>`: daftar dengan nomor (1, 2, 3 atau a, b, c via type)
- Atribut `type` pada ol: "1", "A", "a", "I", "i"
- Atribut `start` pada ol: memulai dari angka tertentu
- `<dl>` + `<dt>` (term) + `<dd>` (definition): daftar definisi
- List bisa bersarang (nested) untuk hierarki

## Kode Implementasi (Gabungan) - HTML, CSS, JavaScript
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HTML List Demo</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <div class="list-demo">
    <h1>Jenis-Jenis List HTML</h1>

    <h2>Unordered List (ul)</h2>
    <ul>
      <li>Apel</li>
      <li>Pisang</li>
      <li>Jeruk</li>
      <li>Anggur</li>
    </ul>

    <h2>Ordered List (ol)</h2>
    <ol>
      <li>Buka VS Code</li>
      <li>Buat file baru</li>
      <li>Simpan sebagai index.html</li>
      <li>Mulai menulis kode</li>
    </ol>

    <h2>Ordered List dengan Type A</h2>
    <ol type="A">
      <li>Pendahuluan</li>
      <li>Isi</li>
      <li>Kesimpulan</li>
    </ol>

    <h2>Nested List</h2>
    <ul>
      <li>Frontend
        <ul>
          <li>HTML</li>
          <li>CSS</li>
          <li>JavaScript</li>
        </ul>
      </li>
      <li>Backend
        <ul>
          <li>Node.js</li>
          <li>Python</li>
        </ul>
      </li>
    </ul>

    <h2>Definition List (dl)</h2>
    <dl>
      <dt>HTML</dt>
      <dd>HyperText Markup Language - bahasa markup untuk struktur halaman web</dd>
      <dt>CSS</dt>
      <dd>Cascading Style Sheets - bahasa untuk styling tampilan web</dd>
      <dt>JavaScript</dt>
      <dd>Bahasa pemrograman untuk interaktivitas web</dd>
    </dl>
  </div>
</body>
</html>
```
```css
body { font-family: Arial, sans-serif; max-width: 700px; margin: 40px auto; padding: 20px; background: #f5f6fa; }
.list-demo { background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); }
h1 { color: #2c3e50; }
h2 { color: #34495e; margin-top: 30px; border-bottom: 1px solid #eee; padding-bottom: 8px; }
ul, ol { line-height: 1.8; }
dl dt { font-weight: bold; color: #2c3e50; margin-top: 10px; }
dl dd { margin-left: 20px; color: #555; }
```

## Cara Integrasi
1. Gunakan `<ul>` untuk daftar tanpa urutan penting
2. Gunakan `<ol>` untuk instruksi/langkah berurutan
3. Gunakan `<dl>` untuk glosarium atau pasangan label-nilai
4. List bisa di-*style* dengan CSS `list-style-type` untuk mengubah bullet/nomor
5. Navigasi website umumnya menggunakan `ul > li > a`
