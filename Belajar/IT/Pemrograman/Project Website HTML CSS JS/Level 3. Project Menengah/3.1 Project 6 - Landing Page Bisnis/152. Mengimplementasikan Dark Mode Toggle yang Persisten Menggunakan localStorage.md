# 152. Mengimplementasikan Dark Mode Toggle yang Persisten Menggunakan localStorage
## Definisi & Target
Dark mode toggle memungkinkan pengguna beralih tema terang/gelap. Targetnya adalah preferensi tema yang tersimpan di localStorage sehingga persisten antar kunjungan.

## Analogi Alur
Seperti saklar lampu kamar yang tetap pada posisi terakhir Anda tinggalkan, bukan kembali ke posisi default setiap kali masuk.

## Poin-Poin Penting
- Simpan preferensi tema di localStorage dengan key tertentu
- Gunakan CSS variables untuk tema agar perpindahan mulus
- Deteksi preferensi sistem dengan `prefers-color-scheme`
- Toggle button dengan icon matahari/bulan
- Tambahkan transisi CSS untuk perpindahan tema yang halus
- Terapkan tema sebelum halaman di-render untuk menghindari flash

## Kode Implementasi (Gabungan) - HTML, CSS, JavaScript
```html
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dark Mode</title>
  <style>
    :root { --bg: #ffffff; --text: #0f172a; --card: #f8fafc; --border: #e2e8f0; --nav-bg: #ffffff; --primary: #2563eb; --muted: #64748b; }
    [data-theme="dark"] { --bg: #0f172a; --text: #f1f5f9; --card: #1e293b; --border: #334155; --nav-bg: #1e293b; --primary: #60a5fa; --muted: #94a3b8; }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: system-ui, sans-serif; background: var(--bg); color: var(--text); transition: background 0.3s, color 0.3s; }
    nav { background: var(--nav-bg); border-bottom: 1px solid var(--border); padding: 16px 40px; display: flex; justify-content: space-between; align-items: center; transition: background 0.3s; }
    .logo { font-weight: 800; font-size: 1.25rem; }
    .theme-toggle { background: none; border: 2px solid var(--border); border-radius: 30px; padding: 8px 16px; cursor: pointer; color: var(--text); font-size: 1.1rem; display: flex; align-items: center; gap: 8px; transition: all 0.3s; }
    .theme-toggle:hover { border-color: var(--primary); }
    .content { max-width: 800px; margin: 0 auto; padding: 60px 40px; }
    .content h1 { font-size: 2.5rem; margin-bottom: 20px; }
    .content p { color: var(--muted); line-height: 1.7; margin-bottom: 16px; }
    .card { background: var(--card); border: 1px solid var(--border); border-radius: 12px; padding: 24px; margin: 20px 0; transition: background 0.3s, border 0.3s; }
    .card h3 { margin-bottom: 8px; }
    .btn { display: inline-block; padding: 12px 24px; background: var(--primary); color: white; border: none; border-radius: 8px; cursor: pointer; font-weight: 600; }
  </style>
</head>
<body>
  <nav>
    <div class="logo">BisnisKu</div>
    <button class="theme-toggle" id="themeToggle" aria-label="Toggle theme">
      <span id="themeIcon">☀️</span>
      <span id="themeLabel">Light</span>
    </button>
  </nav>
  <div class="content">
    <h1>Dark Mode Toggle</h1>
    <p>Klik tombol di pojok kanan atas untuk beralih antara tema terang dan gelap. Preferensi akan disimpan dan tetap aktif meskipun halaman di-refresh.</p>
    <div class="card">
      <h3>Fitur Unggulan</h3>
      <p>Dark mode membantu mengurangi ketegangan mata saat browsing di malam hari dan menghemat baterai pada layar OLED.</p>
    </div>
    <button class="btn">Mulai Sekarang</button>
  </div>
  <script>
    const themeToggle = document.getElementById('themeToggle');
    const themeIcon = document.getElementById('themeIcon');
    const themeLabel = document.getElementById('themeLabel');
    function setTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      localStorage.setItem('theme', theme);
      themeIcon.textContent = theme === 'dark' ? '🌙' : '☀️';
      themeLabel.textContent = theme === 'dark' ? 'Dark' : 'Light';
    }
    const savedTheme = localStorage.getItem('theme');
    const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    const initialTheme = savedTheme || (systemPrefersDark ? 'dark' : 'light');
    setTheme(initialTheme);
    themeToggle.addEventListener('click', () => {
      const current = document.documentElement.getAttribute('data-theme');
      setTheme(current === 'dark' ? 'light' : 'dark');
    });
  </script>
</body>
</html>
```
## Cara Integrasi
Tambahkan CSS variables tema gelap di `:root` dan `[data-theme="dark"]`. Letakkan script theme toggle di `<head>` atau awal `<body>` untuk mencegah flash. Gunakan localStorage untuk persistensi.
