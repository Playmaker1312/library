# 374. Mengimplementasikan Volume Control dengan Slider

## Definisi & Target
Membuat volume control menggunakan slider (input range) untuk mengatur volume pemutaran. Targetnya adalah kontrol volume yang mudah digunakan.

## Analogi Alur
Seperti kenop volume di radio yang bisa diputar untuk mengeraskan atau mengecilkan suara. Slider memberikan kontrol yang presisi.

## Poin-Poin Penting
- Input range dengan nilai 0-100
- Ikon speaker yang berubah sesuai volume
- Volume dihubungkan ke audio.volume (0.0 - 1.0)
- Volume default 0.7

## Kode Implementasi (Gabungan) - HTML, CSS, JavaScript
```html
<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Volume Control</title>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: #0a0a1a; display: flex; justify-content: center; align-items: center; min-height: 100vh; }
.player { background: #1a1a3e; border-radius: 20px; padding: 28px; width: 340px; text-align: center; }
.volume-row { display: flex; align-items: center; gap: 10px; margin: 16px 0; }
.volume-row span { color: #888; font-size: 20px; width: 24px; text-align: center; }
.volume-row input[type="range"] { flex: 1; -webkit-appearance: none; height: 4px; background: #2a2a5e; border-radius: 2px; outline: none; }
.volume-row input[type="range"]::-webkit-slider-thumb { -webkit-appearance: none; width: 16px; height: 16px; border-radius: 50%; background: #4CAF50; cursor: pointer; }
.controls { margin-top: 12px; }
#playBtn { width: 52px; height: 52px; border-radius: 50%; background: #4CAF50; color: #fff; border: none; font-size: 22px; cursor: pointer; }
</style>
</head>
<body>
<div class="player">
<div class="volume-row"><span id="volIcon">🔊</span><input type="range" id="volumeSlider" min="0" max="100" value="70"></div>
<div class="controls"><button id="playBtn">▶</button></div>
</div>
<audio id="audio" src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3"></audio>
<script>
const audio = document.getElementById('audio');
const volumeSlider = document.getElementById('volumeSlider');
const volIcon = document.getElementById('volIcon');
const playBtn = document.getElementById('playBtn');
audio.volume = 0.7;
volumeSlider.addEventListener('input', function() {
const val = this.value / 100;
audio.volume = val;
volIcon.textContent = val === 0 ? '🔇' : val < 0.3 ? '🔈' : val < 0.7 ? '🔉' : '🔊';
});
playBtn.addEventListener('click', () => { if (audio.paused) { audio.play(); playBtn.textContent = '⏸'; } else { audio.pause(); playBtn.textContent = '▶'; } });
</script>
</body>
</html>
```

## Cara Integrasi
Slider volume mengubah properti audio.volume (0-1). Ikon speaker berubah sesuai level volume: 🔇 (0), 🔈 (<30%), 🔊 (>=70%).
